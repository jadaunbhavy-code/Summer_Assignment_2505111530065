#include <stdio.h>
int main() {
    int num, prod = 1;
    printf("Enter number: ");
    scanf("%d", &num);
    if (num == 0) prod = 0;
    while(num > 0) {
        prod *= (num % 10);
        num /= 10;
    }
    printf("Product of digits: %d\n", prod);
    return 0;
}
