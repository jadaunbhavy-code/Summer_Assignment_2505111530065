#include <stdio.h>

int main() {
    int arr[] = {1, 2, 3, 4, 5}, n = 5, sum = 0;
    for (int i = 0; i < n; i++) sum += arr[i];
    
    printf("Sum: %d\n", sum);
    printf("Average: %.2f\n", (float)sum / n);
    return 0;
}
