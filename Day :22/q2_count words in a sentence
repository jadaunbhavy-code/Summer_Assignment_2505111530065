#include <stdio.h>
int main() {
    char str[] = "Learn C programming easily";
    int words = 0;
    for(int i = 0; str[i] != '\0'; i++) {
        if(str[i] == ' ' && str[i+1] != ' ' && str[i+1] != '\0') words++;
    }
    printf("Words: %d", words + 1);
    return 0;
}
