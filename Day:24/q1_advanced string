#include <stdio.h>
#include <string.h>
int main() {
    char s1[] = "abcd", s2[] = "cdab", temp[20] = "";
    if(strlen(s1) == strlen(s2)) {
        strcat(temp, s1);
        strcat(temp, s1);
        if(strstr(temp, s2)) printf("Is a rotation");
        else printf("Not a rotation");
    } else printf("Not a rotation");
    return 0;
}
